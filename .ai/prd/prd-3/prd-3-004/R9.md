---
id: R9
prd: prd-3-004
title: 清理死代码 + 接口合并
acceptance: 删除 src/index.ts 的 MyDurableObject 死代码后 wrangler deploy 通过；/api/* 全部路由正常；相关只读接口已合并（如 sponsors/teams 合并进 snapshot 或保留独立但确认必要）；资源占用较清理前下降
proposal: 删除 MyDurableObject 及无用 Durable Object 迁移配置；审查现有 /api/* 路由，把可合并的只读接口合并，减少请求数；清理后回归验证
status:
  prd: done
  plan: pending
  implement: pending
  test: pending
  review: pending
---

# R9 · 清理死代码 + 接口合并

## 学习要点
1. **死代码**：模板残留的 MyDurableObject 无调用，删除可避免误触发 DO 计费
2. **接口合并**：多个只读接口合并成一个 snapshot，请求数下降、可统一缓存
3. **回归验证**：清理后全 API 回归测试，确保不破坏功能

## 验收
- MyDurableObject 及相关 migration 配置删除
- wrangler deploy 通过
- /api/* 全路由回归正常
- 合并后前端仍能拿到所需数据

## 执行方式
- executor: human（手动实现 + 验证，AI 提供草案与讲解）
- 测试：`.ai/prd/prd-3/TESTPLAN.md` → GATE E（E-5~E-8）
