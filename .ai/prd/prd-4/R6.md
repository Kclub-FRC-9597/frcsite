---
id: R6
prd: prd-4
title: 收尾验证
acceptance: changelog 新增「任务级梯度」记录；R1~R5 全部文件语法校验通过；浏览器全站回归无异常；git 提交存档
proposal: 更新 changelog.js；执行全量语法校验与浏览器回归（GATE 5）；完成 git 提交
status:
  prd: done
  plan: pending
  implement: pending
  test: pending
  review: pending
---

# R6 · 收尾验证

## 目标

PRD 全部落地后的收尾：changelog、全量校验、回归、提交。

## 内容

1. `changelog.js` 新增 2026-08-10 条目：移除「目标设定」记录类型 + 新增任务级目标梯度。
2. 全量语法校验（`node --check` + 内联脚本）。
3. 浏览器回归（集训/学员/统计/任务管理）。
4. git 提交（含 `.ai/prd/prd-4/` 文档）。

## 涉及文件

- `public/inspire/changelog.js`
- `.ai/prd/prd-4/*`（文档）

## 验收要点

1. changelog 有对应记录。
2. GATE 5 全过。
