---
id: prd-4
title: 目标管理改造（移除目标设定 + 任务满分用时目标）
tags: [inspire, 目标管理, 满分用时, 集训, 前端, localStorage]
status: approved
approved_by: 用户
approved_date: 2026-08-10
summary: 移除「目标设定」记录类型（定点删除，保留 __default__ 概览修复），将目标简化为任务级「满分前提下的用时区间」：老师为每个任务设置一个用时区间，学员满分且用时落在区间内即达标；全员一致、可后续修改；学员覆盖纳入范围（首次训练绘制个人基准线时需按学员单独设目标）。
structure:
  - type: r
    id: R1
    title: 移除目标设定
    file: R1.md
  - type: r
    id: R2
    title: 满分用时判定
    file: R2.md
  - type: r
    id: R3
    title: 任务用时配置
    file: R3.md
  - type: r
    id: R4
    title: 集训达标展示
    file: R4.md
  - type: r
    id: R5
    title: 学员达标展示
    file: R5.md
  - type: r
    id: R6
    title: 收尾验证
    file: R6.md
---

# prd-4 · 目标管理改造（移除目标设定 + 任务满分用时目标）

## 背景

「目标设定」最初以**记录类型**（`competitionType='goal'`）实现。该模型不适合"动态管理目标"：

- 目标是数值却被当成"打过分的比赛记录"录入，语义错位；
- 没有"目标 vs 实际"的对照，无法体现达标/未达标、差距；
- 调整目标需重建记录，无法频繁调整与留痕；
- 为让目标成绩"不计入统计"，需在多个页面到处过滤（最佳成绩/综合最佳/排名/学员成绩卡/统计页/趋势图），维护成本高。

**决策**：移除「目标设定」记录类型；把目标简化为**任务级「满分前提下的用时区间」**，老师为每个任务设置一个用时区间，全员共用、创建任务时设定、后续可改。

## 范围

| 需求 | R | 说明 |
|------|---|------|
| A 移除「目标设定」 | R1 | 定点删除记录类型（入口 + 内部实现） |
| B 任务满分用时目标 | R2 | 数据模型 `task.goalTimeRange` + 判定函数 `Shared.evaluateTimeGoal` |
| | R3 | 任务管理页用时区间配置 UI（`tasks.html`） |
| | R4 | 集训统计展示（`training.html` 每个学员最佳成绩下显示下一目标 + 达标徽章/学员覆盖） |
| | R5 | 学员详情与统计页展示（`student.js`/`stats.html`） |
| C 收尾 | R6 | changelog + 全量语法校验 + 浏览器回归 + 提交 |

## 数据模型（核心）

```js
// task 上：满分前提下的用时区间（一个区间，无需命名）
task.goalTimeRange = {
  minTime: null,  // 用时下限（秒，可选，通常留空）
  maxTime: 12     // 用时上限（秒）
}
// 集训上：学员覆盖（可选，缺省走任务默认区间）
training.studentGoals = [ { id, studentId, taskId, goalTimeRange: {/* 学员专属 */} } ]
```

判定：学员**任意一次成绩 得分 = 该任务满分(maxScore) 且 用时落在 [minTime, maxTime] 区间内 → 达标**；否则未达标。未配置区间 / 旧任务 → 不参与判定。

## 决策（已定稿）

- ✅ 判定依据 = **满分前提下的用时区间**（得分必须满分 + 用时落在老师设置的区间内）；单一区间、无需命名、无多档。
- ✅ 学员覆盖 = **保留**（`training.studentGoals`）：**首次训练绘制个人基准线时需按学员单独设目标**，故纳入范围；未设置覆盖的学员走任务默认区间。

## 推进阶段（GATE）

| 阶段 | R | 验证 |
|------|---|------|
| P0② | R1 | 定点删除后下拉只剩 模拟赛/正赛、轮次编号回归、语法校验 |
| P1 | R2 | 判定函数单测（满分非满分/区间边界/无配置） |
| P2 | R3 | 用时区间配置持久化、刷新保留 |
| P3 | R4 | 最佳下方显示下一目标/达标徽章/学员覆盖 |
| P4 | R5 | 学员详情达标+差秒数、统计页达标率 |
| P5 | R6 | changelog + 全量校验 + 浏览器回归 + 提交 |

测试见 `TESTPLAN.md`（GATE 1~5）。P0①（git 安全点提交）已完成：`46389d3`。
