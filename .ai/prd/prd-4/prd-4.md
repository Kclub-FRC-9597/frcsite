---
id: prd-4
title: 目标管理改造（移除目标设定 + 任务满分用时目标）
tags: [inspire, 目标管理, 满分用时, 集训, 前端, localStorage]
status: approved
approved_by: 用户
approved_date: 2026-08-10
summary: 移除「目标设定」记录类型（定点删除，保留 __default__ 概览修复），将目标简化为任务级「满分前提下的目标时间档位列表」（如 100s/80s/70s）：老师为每个任务设定目标档位，学员满分且用时 ≤ 档位即达成，当前目标 = 下一档更快目标；全员一致、可后续修改；学员覆盖纳入范围（首次训练绘制个人基准线时需按学员单独设目标）。
structure:
  - type: r
    id: R1
    title: 移除目标设定
    file: R1.md
  - type: r
    id: R2
    title: 满分用时判定
    file: R2.md
  - type: r
    id: R3
    title: 任务用时配置
    file: R3.md
  - type: r
    id: R4
    title: 集训达标展示
    file: R4.md
  - type: r
    id: R5
    title: 个人数据展示
    file: R5.md
  - type: r
    id: R6
    title: 收尾验证
    file: R6.md
---

# prd-4 · 目标管理改造（移除目标设定 + 任务满分用时目标）

## 背景

「目标设定」最初以**记录类型**（`competitionType='goal'`）实现。该模型不适合"动态管理目标"：

- 目标是数值却被当成"打过分的比赛记录"录入，语义错位；
- 没有"目标 vs 实际"的对照，无法体现达标/未达标、差距；
- 调整目标需重建记录，无法频繁调整与留痕；
- 为让目标成绩"不计入统计"，需在多个页面到处过滤（最佳成绩/综合最佳/排名/学员成绩卡/统计页/趋势图），维护成本高。

**决策**：移除「目标设定」记录类型；把目标简化为**任务级「满分前提下的目标时间档位列表」**，老师为每个任务设置目标档位（如 100s/80s/70s），全员共用、创建任务时设定、后续可改。

## 范围

| 需求 | R | 说明 |
|------|---|------|
| A 移除「目标设定」 | R1 | 定点删除记录类型（入口 + 内部实现） |
| B 任务满分用时目标 | R2 | 数据模型 `task.goalTimes`（时间档位列表）+ 判定函数 `Shared.currentGoal / isGoalAchieved` |
| | R3 | 任务管理页目标时间档位配置 UI（`tasks.html`） |
| | R4 | 集训统计展示（`training.html` 每个学员最佳成绩下显示当前目标 + 达标徽章/学员覆盖） |
| | R5 | 个人成绩卡展示（`student.js` 全部数据 + 自主/模拟分开-整合切换 + 达标/当前目标） |
| C 收尾 | R6 | changelog + 全量语法校验 + 浏览器回归 + 提交 |

## 数据模型（核心）

```js
// task 上：目标时间档位列表（从松到紧，越低越难；达成需 满分 + 用时 ≤ 档位）
task.goalTimes = [100, 80, 70]   // 秒；创建任务时统一设定，可后续调整
// 集训上：学员覆盖（可选，缺省走任务默认档位）
training.studentGoals = [ { id, studentId, taskId, goalTimes: [/* 学员专属档位 */] } ]
```

判定（满分前提）：某档目标 t **达成** = 学员有成绩 得分=满分(maxScore) 且 用时 ≤ t。
**当前目标** = 取尚未达成且最接近的下一档：当前最佳满分用时 best，`当前目标 = max{ t ∈ goalTimes | t < best }`；best 75s → 当前目标 70s；无满分成绩 → 目标为最松档（max goalTimes）；全部达成 → 无当前目标。

## 决策（已定稿）

- ✅ 目标形态 = **时间档位列表**（如 100s/80s/70s），非区间、无需命名；达成需**满分前提**（得分=满分 且 用时 ≤ 档位时间）。
- ✅ 当前目标 = 从档位取**下一档更快目标**（当前最佳满分用时 75s → 当前目标 70s）；任务创建时统一设定、可后续调整。
- ✅ 学员覆盖 = **保留**（`training.studentGoals`）：**首次训练绘制个人基准线时需按学员单独设目标**，故纳入范围；未设置覆盖的学员走任务默认档位。

## 数据边界（展示来源）

- **集训页**（`training.html`）：只展示/统计 **模拟赛/正赛**（`mockCompetitions`）；自主练习（`practiceRecords`）**不进入**集训统计与排名。目标/达标判定仅基于模拟赛/正赛成绩。
- **个人成绩卡**（`student.js`）：展示**全部数据**（模拟赛 + 正赛 + 自主练习）；提供「自主练习 / 模拟练习」的**分开 / 整合**切换预览；模拟练习 = 模拟赛 + 正赛。

## 推进阶段（GATE）

| 阶段 | R | 验证 |
|------|---|------|
| P0② | R1 | 定点删除后下拉只剩 模拟赛/正赛、轮次编号回归、语法校验 |
| P1 | R2 | 判定函数单测（满分非满分/档位边界/无配置/全达成） |
| P2 | R3 | 目标档位列表配置持久化、刷新保留 |
| P3 | R4 | 最佳下方显示当前目标/达标徽章/学员覆盖 |
| P4 | R5 | 个人成绩卡全部数据+自主/模拟切换、达标/当前目标 |
| P5 | R6 | changelog + 全量校验 + 浏览器回归 + 提交 |

测试见 `TESTPLAN.md`（GATE 1~5）。P0①（git 安全点提交）已完成：`46389d3`。
