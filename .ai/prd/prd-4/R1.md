---
id: R1
prd: prd-4
title: 移除目标设定
acceptance: 新增记录弹窗记录类型只剩 模拟赛/正赛；新建模拟赛默认名回到「第N轮」（目标设定不再占用编号）；training.html/shared.js/student.js/stats.html/changelog.js 中 goal 相关实现全部删除；`Shared.getMockTypeText/Label/Bg` 无 goal 分支且 `isStatCounted` 已删；`__default__` 概览兜底修复保留；`node --check` 与内联脚本校验通过
proposal: 定点删除 5 文件中的 goal 相关行（清单见 R1.plan.yaml），保留 __default__ 兜底与 getMockType* 重构；已建 goal 记录保留在 localStorage 作历史，不迁移不清理
status:
  prd: done
  plan: pending
  implement: pending
  test: pending
  review: pending
---

# R1 · 移除目标设定

## 目标

删除「目标设定」记录类型（`competitionType='goal'`）的所有入口与内部实现，回到只含 模拟赛/正赛 的干净状态；同时**保留** `__default__` 概览兜底修复与 `Shared.getMockType*` 徽章/文案重构。

## 涉及文件

| 文件 | 删点 |
|------|------|
| `public/inspire/training.html` | 下拉 `<option value="goal">🎯 目标设定</option>`；`confirmMockModal` 的 goal 默认命名与 `isStatCounted` 计数（还原为 `training.mockCompetitions.length`）；`addMockCompetition` 的 `isStatCounted` 计数与注释；`renderTrainingList` 的 `statMocks` 过滤与 goal 注释、占位 else 分支；`renderStudentDetail` 的 `chartRecords` 过滤与两处 `if (!Shared.isStatCounted(mock)) return;` |
| `public/inspire/shared.js` | `getMockTypeText/Label/Bg` 去掉 goal 分支（只保留 模拟赛/正赛）；删除 `isStatCounted` |
| `public/inspire/student.js` | 删除 `if (mockSource === 'goal') return;` |
| `public/inspire/stats.html` | 删除 `if (Shared.getMockType(m) === 'goal') return;` |
| `public/inspire/changelog.js` | 删除 2026-08-10「记录类型新增目标设定」条目 |

## 验收要点

1. 下拉只剩 模拟赛/正赛；默认名回到「第N轮」。
2. 统计概览不再有 goal 过滤，`__default__` 兜底仍生效（无任务记录也能进最佳成绩概览）。
3. 5 个文件语法校验通过，浏览器回归无异常。
