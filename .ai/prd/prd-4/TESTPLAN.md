# TESTPLAN · prd-4 目标管理改造

## GATE 1（R1 移除目标设定）

### 前置条件
- [agent] 代码已按 R1.plan.yaml 删除完成
- [human] 浏览器已打开训练管理页（http://127.0.0.1:8787/inspire/training.html）

### 自动化检查 [agent 执行]
| 编号 | 验证内容 | 命令 | 预期结果 |
|------|---------|------|---------|
| A-1 | shared.js 语法 | `node --check public/inspire/shared.js` | 无输出 |
| A-2 | student.js 语法 | `node --check public/inspire/student.js` | 无输出 |
| A-3 | changelog.js 语法 | `node --check public/inspire/changelog.js` | 无输出 |
| A-4 | training.html 内联脚本 | node 提取无 src 的 `<script>` 逐个校验 | ALL OK |
| A-5 | stats.html 内联脚本 | 同上 | ALL OK |

### 手工验证 [human 执行]
| 编号 | 操作步骤 | 预期结果 |
|------|---------|---------|
| M-1 | 新增记录，展开「记录类型」下拉 | 只剩 模拟赛/正赛，无 目标设定 |
| M-2 | 名称留空新增一条模拟赛 | 默认名「第N轮」，且编号不被之前的目标设定占用 |
| M-3 | 打开含「无任务但有成绩」记录的集训 | 最佳成绩概览仍显示真实成绩（`__default__` 兜底生效） |
| M-4 | 打开学员详情 / 统计页 / 导出 CSV | 正常，无 goal 相关残留 |

### GATE 1 通过条件
- [ ] [agent] A-1~A-5 全部通过
- [ ] [human] M-1~M-4 全部通过，结果已反馈

---

## GATE 2（R2 满分用时判定）

### 前置条件
- [agent] `task.goalTimeRange` + `Shared.evaluateTimeGoal` 已实现

### 自动化检查 [agent 执行]
| 编号 | 验证内容 | 命令 | 预期结果 |
|------|---------|------|---------|
| A-1 | 判定：满分且在区间内 | 注入测试调用 evaluateTimeGoal | 达标（true） |
| A-2 | 判定：满分但超时 | 注入测试 | 未达标（false） |
| A-3 | 判定：非满分 | 注入测试 | 未达标（false） |
| A-4 | 旧任务无 goalTimeRange | 注入测试 | 不参与（null），不影响统计 |

### GATE 2 通过条件
- [ ] [agent] A-1~A-4 全部通过

---

## GATE 3（R3 任务用时配置）

### 手工验证 [human 执行]
| 编号 | 操作步骤 | 预期结果 |
|------|---------|---------|
| M-1 | 任务管理创建任务，设置用时区间（上限必填/下限可留空） | 可保存，说明文案显示 |
| M-2 | 刷新页面 | 用时区间保留（localStorage） |
| M-3 | 编辑任务修改区间 | 保存后即时生效 |

### GATE 3 通过条件
- [ ] [human] M-1~M-3 全部通过，结果已反馈

---

## GATE 4（R4 集训达标展示 / R5 学员达标展示）

### 手工验证 [human 执行]
| 编号 | 操作步骤 | 预期结果 |
|------|---------|---------|
| M-1 | 集训详情最佳成绩旁 | 显示达标徽章 ✅/✗，概览有达标人数 + 达标率 |
| M-2 | 学员「调整目标」 | 可覆盖用时区间，仅影响该学员 |
| M-3 | 学员详情每任务 | 显示达标 + 差几秒（或未满分提示） |
| M-4 | 统计页 | 显示达标率 |

### GATE 4 通过条件
- [ ] [human] M-1~M-4 全部通过，结果已反馈

---

## GATE 5（R6 收尾验证）

### 自动化检查 [agent 执行]
| 编号 | 验证内容 | 命令 | 预期结果 |
|------|---------|------|---------|
| A-1 | 全量语法校验 | 5 个涉及文件 | 全部通过 |
| A-2 | changelog 含「满分用时目标」条目 | 检查 changelog.js | 已新增 |

### 手工验证 [human 执行]
| 编号 | 操作步骤 | 预期结果 |
|------|---------|---------|
| M-1 | 全站浏览器回归（集训/学员/统计/任务管理） | 无异常 |

### GATE 5 通过条件
- [ ] [agent] A-1~A-2 通过
- [ ] [human] M-1 通过，结果已反馈
