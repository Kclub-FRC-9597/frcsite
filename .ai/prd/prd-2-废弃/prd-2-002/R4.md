---
id: R4
prd: prd-2-002
title: 只读接口 GET /api/snapshot
acceptance: 公网（手机流量）GET /api/snapshot 返回 200；返回结构与导入数据一致（学员/任务/集训/模拟赛/成绩齐全）；无 token 可访问；数据量小一次拉全量即可
proposal: 在 modules/inspire/routes.ts 实现 snapshot handler：按契约查询全部表并组装返回；只读公开
status:
  prd: done
  plan: pending
  implement: pending
  test: pending
  review: pending
---

# R4 · 只读接口 /api/snapshot

## 学习要点
1. **第一个 GET handler**：接收请求 → 查库 → 返回 JSON
2. **pg 查询**：`pool.query(sql)`，参数化避免注入
3. **公开接口**：无鉴权，学员/家长可用

## 验收
- 空库 snapshot → 200 空结构
- 导入后 snapshot → 与数据库一致
- 公网可访问（GATE D 端到端）

## 执行方式
- executor: human（手动实现 + 验证，AI 提供草案与讲解）
- 测试：`.ai/prd/TESTPLAN.md` → GATE C（C-3、C-4、C-7）+ GATE D
