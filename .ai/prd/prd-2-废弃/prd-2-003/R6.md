---
id: R6
prd: prd-2-003
title: Inspire 前端接入
acceptance: 在 frcsite public/inspire/ 实现：migration.html 可带 token 导入到服务器（写）；stats/display 只读页从 /api/snapshot 加载并展示服务器数据；学员/家长视角只读、教练可写；多设备端到端可用（GATE D）
proposal: 前端增加 API 客户端封装（fetch /api/snapshot、POST /api/import），按契约处理 CORS 与 token；后端地址可配置
status:
  prd: done
  plan: pending
  implement: pending
  test: pending
  review: pending
---

# R6 · Inspire 前端接入

## 学习要点
1. **fetch**：浏览器发 HTTP 请求的标准方式
2. **CORS**：前端（inspire.frc9597.com）与后端（api.inspire.frc9597.com）跨域，后端需允许
3. **前后端联调**：先看契约，再各做各的，最后联调

## 验收
- 教练在 migration.html 导入 → 服务器收到数据
- 学员/家长设备刷新 stats/display → 显示服务器数据
- 多设备并发访问无 429（GATE D M-D3）
- 学员/家长无 token 写被拒（GATE D M-D5）

## 执行方式
- executor: human（手动实现 + 验证，AI 提供草案与讲解）
- 测试：`.ai/prd/TESTPLAN.md` → GATE D（全手工、跨设备）
