---
id: prd-2-003
parent: prd-2
title: 前端接入
status: draft
summary: 在 frcsite 的 inspire 静态站（public/inspire/）按契约接入后端：管理端导入入口 + 只读页读取，实现端到端多设备访问。
structure:
  - type: r
    id: R6
    title: Inspire 前端接入
    file: R6.md
---

# prd-2-003 · 前端接入

## 本子组目标
让 inspire 静态站（仍在 frc9597.com / inspire.frc9597.com 发布，**不动部署**）通过 fetch 调用后端，实现：
- 教练/管理员：`migration.html` 增加"导入到服务器"入口（写）
- 学员/家长：只读页（stats/display）读取 `/api/snapshot` 展示服务器数据

## 边界
- 前端改动在 **frcsite 仓库** `public/inspire/`，与后端（frc9597-backend）解耦
- 前后端只通过 `.ai/prd/prd-2/docs/api-contract.md` 契约耦合
